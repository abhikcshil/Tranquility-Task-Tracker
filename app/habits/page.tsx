import { PageShell } from '@/components/page-shell';

export default function HabitsPage() {
  return (
    <PageShell title="Habits" description="Habit tracking and streaks will be added in a future phase.">
      <p className="text-sm text-zinc-300">Habits placeholder.</p>
    </PageShell>
  );
}
